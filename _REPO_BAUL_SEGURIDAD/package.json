{
  "name": "fidelidad-next-monorepo",
  "private": true,
  "scripts": {
    "build": "cd fidelidad-next && npm install && npm run build"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "nodemailer": "^7.0.12"
  }
}